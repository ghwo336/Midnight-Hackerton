{
  "name": "once-finance",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build:contract": "pnpm --filter @once/contract build",
    "test": "vitest run",
    "test:attacks": "vitest run test/attacks",
    "typecheck": "tsc -b",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "@types/node": "^22.13.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
