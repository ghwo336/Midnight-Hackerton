node_modules/
dist/
build/
.next/
.turbo/
.data/
coverage/
*.log
.env
.env.local
contracts/out/

# contracts/managed/ 는 의도적으로 커밋한다.
# 심사위원이 Compact 툴체인 설치 없이 클론만으로 데모를 돌릴 수 있어야 한다.
# 증명키 4개 포함 21MB, 최대 파일 9.5MB.
.DS_Store
.vscode/

# ZK 자산은 contracts/managed/에서 복사해 온 것이다.
# pnpm --filter @once/web sync:zk 로 다시 만든다.
apps/web/public/zk/

# 발급 기관 키 내보내기 파일.
# 이 파일을 가진 사람은 해당 컨트랙트에 채권을 발급할 수 있다.
# 실수로 커밋되는 경로를 애초에 막는다.
once-issuer-key-*.json
